server {
    listen 80;
    server_name ritiandong.com;

    # 可选：将 HTTP 重定向到 HTTPS（如果配置了 SSL）
    # return 301 https://$server_name$request_uri;

    # 聊天室前端
    location /chat {
        alias /home/li7808/forlearning/claude/chatroom/frontend;
        index index.html;
        try_files $uri $uri/ /chat/index.html;
    }

    # API 反向代理
    location /api {
        proxy_pass http://127.0.0.1:8000/api;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # WebSocket 反向代理
    location /ws {
        proxy_pass http://127.0.0.1:8000/ws;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400;
    }

    # 上传的文件
    location /uploads {
        proxy_pass http://127.0.0.1:8000/uploads;
    }
}

# HTTPS 配置（可选，使用 Let's Encrypt）
# server {
#     listen 443 ssl http2;
#     server_name ritiandong.com;
#
#     ssl_certificate /etc/letsencrypt/live/ritiandong.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/ritiandong.com/privkey.pem;
#
#     # 其他配置同上...
# }
